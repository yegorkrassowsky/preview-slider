window.addEventListener('load', function(event) {

	previewSliderInit({
	});

	previewSliderInit({
		listClass: 'list',
		listWrapClass: 'listWrap',
		nextArrowText: '>',
		prevArrowText: '<'
	});


});